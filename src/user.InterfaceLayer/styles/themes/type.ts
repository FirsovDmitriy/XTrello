export interface ITheme {
  backColor: string,
  textColor: string,

  background: {
    primary: string,
    hovered: string,
    disabled: string
  },
  text: {
    disabled: string
  },

  border: {
    primary: string
  }
}
